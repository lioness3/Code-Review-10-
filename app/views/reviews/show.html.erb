<%= render "layouts/company_logo" %>
<%=@product.name %>
<%= %>
<div class="reviews">
  <%= if @product.reviews %>
  <%=@product.reviews.each do |review| %>
  <ul>
    <li><%= review.author %></li>
    <ul>
      <li><%= review.rating %></li>
      <li><p><%= review.content_body %></p></li>
    </ul>
    l
  </ul>
  <%= %>
  <% end %>
  <% end%>

  select first 10 content body
</div>

<%= form_for @products do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
   <%= f.collection_select(:division_id, @division, :id, :name)%>
               <%# choose by rating  %>
  <%= f.submit  %>
<% end %>
<%= render "layouts/company_info" %>
